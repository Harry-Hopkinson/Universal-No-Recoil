#pragma once

constexpr int WINDOW_WIDTH = 1250;
constexpr int WINDOW_HEIGHT = 1000;

extern bool EnableRC;
extern bool Running;
extern bool IsAttackerView;

extern bool EnableController;

extern bool UseToggleKey;

extern const char* CurrentWeapon;
